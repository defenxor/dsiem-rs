{
  "vuln_sources": [
    {
      "name": "Nessus",
      "type": "IP-Port",
      "plugin": "Nesd",
      "enabled": true,
      "config": "{ \"url\" : \"http://127.0.0.1:18082/?ip=${ip}&port=${port}\" }"
    }
  ]
}